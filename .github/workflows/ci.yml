name: CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run tests
        run: python3 -m pytest
      - name: Validate skill
        run: python3 scripts/validate_skill.py .
      - name: Run routing evaluations
        run: python3 scripts/run_evals.py --models-file tests/catalog.json
